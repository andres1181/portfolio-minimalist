<footer class="no-print">
    Pulsa <kbd>ctr</kbd>+<kbd>K</kbd> para mostrar la paleta de comandos.
</footer>
<ninja-keys placeholder="Buscar comando"> </ninja-keys>

<script>
    import "ninja-keys";

    interface INinjaAction {
        id: string;
        title: string;
        hotkey: string;
        icon: string;
        section: string;
        handler: () => void;
    }

    const ninja = document.querySelector("ninja-keys");
    if (ninja != null) {
        const data = [
            {
                id: "print",
                title: "Imprimir",
                hotkey: "ctrl+P",
                icon: `<svg style="margin-right: 8px" width="16"  height="16"  viewBox="0 0 24 24"  fill="none"  stroke="currentColor"  stroke-width="2"  stroke-linecap="round"  stroke-linejoin="round"  class="icon icon-tabler icons-tabler-outline icon-tabler-printer"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M17 17h2a2 2 0 0 0 2 -2v-4a2 2 0 0 0 -2 -2h-14a2 2 0 0 0 -2 2v4a2 2 0 0 0 2 2h2" /><path d="M17 9v-4a2 2 0 0 0 -2 -2h-6a2 2 0 0 0 -2 2v4" /><path d="M7 13m0 2a2 2 0 0 1 2 -2h6a2 2 0 0 1 2 2v4a2 2 0 0 1 -2 2h-6a2 2 0 0 1 -2 -2z" /></svg>`,
                section: "Acciones",
                handler: () => {
                    window.print();
                },
            },
        ] as Array<INinjaAction>;

        ninja.data = data;
    }
</script>
<style>



    @media print {
        ninja-keys {
            display: none !important;
        }
        .no-print {
            display: none !important;
        }
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
        } 
        to {
            opacity: 1;
        }
        
    }

    footer {
        background: #eee;
        border-top: 1px solid #dbdbdb;
        padding-block: 4px;
        position: fixed;
        bottom: 0;
        width: 100%;
        text-align: center;
        font-size: 0.85rem;

        view-timeline-name: --revealing;
        view-timeline-axis: block;

        animation: linear 0.3s fadeIn;
        animation-timeline: --revealing;
        animation-range: entry 100% cover 10%;
    }
    kbd {
        text-align: center;
        border-top: 1px solid #b3b3b3;
        border-radius: 4px;
        width: 100%;
        padding-inline: 4px;
        background: #d6d6d6;
    }
</style>
